import type { BaseErrorType } from 'viem';
import { Actions } from 'viem/tempo';
import type { Config } from '../../createConfig.js';
import type { ChainIdParameter } from '../../types/properties.js';
import type { PartialBy } from '../../types/utils.js';
import type { QueryOptions, QueryParameter } from './utils.js';
/**
 * Gets the nonce for an account and nonce key.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from '@wagmi/core/chains'
 * import { Actions } from '@wagmi/core/tempo'
 *
 * const config = createConfig({
 *   chains: [tempo],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const nonce = await Actions.nonce.getNonce(config, {
 *   account: '0x...',
 *   nonceKey: 1n,
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The nonce value.
 */
export declare function getNonce<config extends Config>(config: config, parameters: getNonce.Parameters<config>): Promise<getNonce.ReturnValue>;
export declare namespace getNonce {
    type Parameters<config extends Config> = ChainIdParameter<config> & Actions.nonce.getNonce.Parameters;
    type ReturnValue = Actions.nonce.getNonce.ReturnValue;
    type ErrorType = BaseErrorType;
    function queryKey<config extends Config>(parameters: PartialBy<Parameters<config>, 'account' | 'nonceKey'>): readonly ["getNonce", PartialBy<Parameters<config>, "account" | "nonceKey">];
    type QueryKey<config extends Config> = ReturnType<typeof queryKey<config>>;
    function queryOptions<config extends Config, selectData = ReturnValue>(config: Config, parameters: queryOptions.Parameters<config, selectData>): queryOptions.ReturnValue<config, selectData>;
    namespace queryOptions {
        type Parameters<config extends Config, selectData = getNonce.ReturnValue> = PartialBy<getNonce.Parameters<config>, 'account' | 'nonceKey'> & QueryParameter<getNonce.ReturnValue, getNonce.ErrorType, selectData, getNonce.QueryKey<config>>;
        type ReturnValue<config extends Config, selectData = getNonce.ReturnValue> = QueryOptions<getNonce.ReturnValue, getNonce.ErrorType, selectData, getNonce.QueryKey<config>>;
    }
}
/**
 * Watches for nonce incremented events.
 *
 * @deprecated This function has been deprecated post-AllegroModerato. It will be removed in a future version.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from '@wagmi/core/chains'
 * import { Actions } from '@wagmi/core/tempo'
 *
 * const config = createConfig({
 *   chains: [tempo],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const unwatch = Actions.nonce.watchNonceIncremented(config, {
 *   onNonceIncremented: (args, log) => {
 *     console.log('Nonce incremented:', args)
 *   },
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns A function to unsubscribe from the event.
 */
export declare function watchNonceIncremented<config extends Config>(config: config, parameters: watchNonceIncremented.Parameters<config>): () => void;
export declare namespace watchNonceIncremented {
    type Parameters<config extends Config> = ChainIdParameter<config> & Actions.nonce.watchNonceIncremented.Parameters;
}
/**
 * Watches for active key count changed events.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from '@wagmi/core/chains'
 * import { Actions } from '@wagmi/core/tempo'
 *
 * const config = createConfig({
 *   chains: [tempo],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const unwatch = Actions.nonce.watchActiveKeyCountChanged(config, {
 *   onActiveKeyCountChanged: (args, log) => {
 *     console.log('Active key count changed:', args)
 *   },
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns A function to unsubscribe from the event.
 */
export declare function watchActiveKeyCountChanged<config extends Config>(config: config, parameters: watchActiveKeyCountChanged.Parameters<config>): () => void;
export declare namespace watchActiveKeyCountChanged {
    type Parameters<config extends Config> = ChainIdParameter<config> & Actions.nonce.watchActiveKeyCountChanged.Parameters;
}
/**
 * Gets the number of active nonce keys for an account.
 *
 * @deprecated This function has been deprecated post-AllegroModerato. It will be removed in a future version.
 *
 * @example
 * ```ts
 * import { createConfig, http } from '@wagmi/core'
 * import { tempo } from '@wagmi/core/chains'
 * import { Actions } from '@wagmi/core/tempo'
 *
 * const config = createConfig({
 *   chains: [tempo],
 *   transports: {
 *     [tempo.id]: http(),
 *   },
 * })
 *
 * const count = await Actions.nonce.getNonceKeyCount(config, {
 *   account: '0x...',
 * })
 * ```
 *
 * @param config - Config.
 * @param parameters - Parameters.
 * @returns The number of active nonce keys.
 */
export declare function getNonceKeyCount<config extends Config>(config: config, parameters: getNonceKeyCount.Parameters<config>): Promise<getNonceKeyCount.ReturnValue>;
export declare namespace getNonceKeyCount {
    type Parameters<config extends Config> = ChainIdParameter<config> & Actions.nonce.getNonceKeyCount.Parameters;
    type ReturnValue = Actions.nonce.getNonceKeyCount.ReturnValue;
    type ErrorType = BaseErrorType;
    function queryKey<config extends Config>(parameters: PartialBy<Parameters<config>, 'account'>): readonly ["getNonceKeyCount", PartialBy<Parameters<config>, "account">];
    type QueryKey<config extends Config> = ReturnType<typeof queryKey<config>>;
    function queryOptions<config extends Config, selectData = ReturnValue>(config: Config, parameters: queryOptions.Parameters<config, selectData>): queryOptions.ReturnValue<config, selectData>;
    namespace queryOptions {
        type Parameters<config extends Config, selectData = getNonceKeyCount.ReturnValue> = PartialBy<getNonceKeyCount.Parameters<config>, 'account'> & QueryParameter<getNonceKeyCount.ReturnValue, getNonceKeyCount.ErrorType, selectData, getNonceKeyCount.QueryKey<config>>;
        type ReturnValue<config extends Config, selectData = getNonceKeyCount.ReturnValue> = QueryOptions<getNonceKeyCount.ReturnValue, getNonceKeyCount.ErrorType, selectData, getNonceKeyCount.QueryKey<config>>;
    }
}
//# sourceMappingURL=nonce.d.ts.map